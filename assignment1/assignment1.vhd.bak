library IEEE;
use IEEE.STD_LOGIC_1164.ALL;

entity combinational is
	PORT( a, b, c : in STD_LOGIC;
			f : out STD_LOGIC
			);
end combinational;


architecture Behavioural of combinational is
begin
	f <= (((NOT a) AND b) OR ((NOT A) AND c) OR ((NOT b) OR c) OR (a OR (NOT b)))